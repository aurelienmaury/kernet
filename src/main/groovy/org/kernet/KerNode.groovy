package org.kernet


class KerNode {

    String selfDomain = 'localhost'

    /*

    adresse de contact via le web
    Map des Kernode (max 32 ou 64)
    Cache des questions envoyées
    signature des questions envoyées, check de la signature à la réception
    onMessage => search local, if nothing, cache origin, swap origin and send to next random


     */


}
